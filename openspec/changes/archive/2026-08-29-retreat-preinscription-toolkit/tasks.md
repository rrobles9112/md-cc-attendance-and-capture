# Tasks: retreat-preinscription-toolkit

| Field | Value |
|---|---|
| Change | `retreat-preinscription-toolkit` |
| Inputs | `proposal.md` · `specs/retreat-preinscription-toolkit/spec.md` (R1–R5, 24 scenarios) · `design.md` (§2–§11, AD-1…AD-16, RK-1…RK-9) |
| Status | Tasks ready — ready for `apply` |
| Delivery | 5 stacked PRs to main (squash): PR #1 (W1) → PR #2 (W2) → PR #3a (W3 generator) → PR #3b (W3 runner) → PR #4 (W4); each < 200 production lines |
| Quality gates | Strict TDD (Vitest, `npx vitest run`, include pattern `src/**/__tests__/**/*.test.{ts,tsx}`; RED → GREEN → TRIANGULATE → REFACTOR) · typecheck `npx tsc --noEmit` · RDD manual 4R per PR with correction budget `min(200, ceil(changed_lines/2))` · Playwright e2e out of scope |
| Test corrections (gatekeeper-authoritative) | `src/lib/pastoreo/__tests__/explain.test.ts`: 2 filename literals (lines 13, 30) `012b_…` → `016_…` + describe title "012b" → "016". `src/__tests__/rls/whatsapp_pastoreo.test.ts`: 3 filename literals (lines 13, 29, 40) `012c_…` → `017_…` + comment "012c" → "017". No test references `012a`/`012d` filenames. |
| Ownership | `implementation` = code/tests/apply-owned verification (PR sections below). `parent` = bounded review, merge/lifecycle gates, ops delivery (final sections). |

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~395–495 production lines in aggregate across 5 PRs (largest single PR ≈ 190); tests/docs additional (~600–750) |
| 400-line budget risk | Low — chained delivery keeps every PR under 200 production lines; the aggregate exceeds 400 only because it is deliberately split (single-PR delivery would be High) |
| Chained PRs recommended | Yes |
| Suggested split | PR #1 (W1 duplicate-email 409) → PR #2 (W2 migration renumber) → PR #3a (W3 seed generator) → PR #3b (W3 seed runner) → PR #4 (W4 Nueva preinscripción modal) |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Per-PR production-line forecast (test/script/doc lines do not count against the 200-line production budget, kept reasonable):

| PR | Production changes | Est. prod lines | Est. test/doc lines |
|---|---|---|---|
| #1 W1 | MOD `src/lib/admin/user-store.ts` only | ~25 | ~160–190 (NEW store test; MOD UsersPanel test; optional service pin) |
| #2 W2 | RENAME 4 files in `supabase/migrations/` (0 content lines) | 0 | ~7 (5 literals + describe + comment in 2 test files) |
| #3a W3a | NEW `src/lib/retreat/seed-cohort.ts` + 1 line in `tsconfig.json` | ~150–190 | ~180–250 (NEW `seed-cohort.test.ts`) |
| #3b W3b | NEW `scripts/retreat/seed-cohort.ts` | ~150–190 | ~80–120 (NEW `docs/retreat-seed-cohort.md`) |
| #4 W4 | NEW `src/components/retreat/RetreatPreinscriptionCreate.tsx` + ~8 lines in the page | ~70–90 | ~150–200 (2 NEW test files) |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Low
```

All delivery decisions are already resolved (stacked-to-main, squash, pre-split #3a/#3b per AD-16). Contingencies RK-1/RK-2 carry decision trees in the Ops section; the #3a/#3b merge option defaults to split.

## Execution order and dependencies

- Complete each PR's implementation tasks in listed order (RED → GREEN → TRIANGULATE → REFACTOR); its parent gate (T-50x) must PASS and merge before the next PR merges (fixed order #1 → #2 → #3a → #3b → #4).
- Code dependencies: PR #3b imports the module created by PR #3a — #3a must be merged (or directly below in the stack) before #3b's verification. PRs #1, #2, #4 are code-independent of each other; the order is delivery policy, not compile order.
- Every PR leaves main buildable with the full Vitest suite green (stacked-PR green rule).
- Ops actions (T-601–T-604) run outside the PRs; S1/S2 (PR #1) and S3/S4 (PR #2) verification depend on ops execution, not on code review.

---

## PR #1 — W1: duplicate-email 409 classifier

Maps to spec **R1 (Hosted Admin User Creation)** scenarios: duplicate email → 409 (never 500); UsersPanel es-CO toast; non-duplicate keeps existing mapping. (Valid-hosted-creation scenario is verified by ops S1/S2 in T-601.) Design: §3.1, §5.1, §7.1–§7.3, AD-1, AD-2. Only production file: `src/lib/admin/user-store.ts` (~25 lines); route/service/api/panel are verified zero-change (§2.1) and must show an empty diff.

- [x] T-101 RED — Create `src/lib/admin/__tests__/user-store.test.ts` (mirror the `src/lib/admin/__tests__/user-service.test.ts` patterns: `vi.mock('@supabase/supabase-js', …)` + `stubEnv`) with the duplicate-email classification cases from design §7.1 (1)–(3): mocked `auth.admin.createUser` rejecting `{ message: 'User already registered' }`, `{ code: 'user_already_exists' }` (wording-variant message), and `{ code: '422', … }` / `{ status: 422, message: '' }` — each must reject with `AdminUserStoreError` kind `conflict` and message `CONFLICT_EMAIL_MESSAGE_ES` imported from `@/lib/admin/user-store` (no re-typed copies). Run `npx vitest run src/lib/admin/__tests__/user-store.test.ts` → confirm RED (exports missing; current code maps everything to `auth_failed`). <!-- sdd-owner: implementation -->
- [x] T-102 GREEN — In `src/lib/admin/user-store.ts`: export `CONFLICT_EMAIL_MESSAGE_ES = 'Ya existe una cuenta registrada con ese correo electrónico.'` and the pure classifier `isDuplicateEmailAuthError(error: unknown): boolean` (AD-1: message regex `/already registered|user_already_exists/i`, OR `code === 'user_already_exists'`, OR (`code === '422'` || `status === 422`) with a message that does not mention "password"); in `createAuthUser`'s error branch throw `AdminUserStoreError('conflict', CONFLICT_EMAIL_MESSAGE_ES)` when the classifier matches, otherwise keep the existing `auth_failed` mapping with the upstream message. Targeted vitest run → GREEN. <!-- sdd-owner: implementation -->
- [x] T-103 TRIANGULATE — Extend `user-store.test.ts` with boundary cases §7.1 (4)–(7): password-422 (`{ status: 422, message: 'Password should be at least 6 characters.' }`) stays `auth_failed` with the upstream message; unrelated failure (`{ message: 'Email not confirmed' }`, status 500) stays `auth_failed`; `isDuplicateEmailAuthError` truth table (null, non-object, `{}`, GoTrue shapes, non-422 statuses → false); success path resolves the created user id. All GREEN with no further production change — if a boundary fails, narrow the classifier (do not widen the conflict branch). <!-- sdd-owner: implementation -->
- [x] T-104 UsersPanel characterization pin — In `src/components/admin/__tests__/UsersPanel.test.tsx`, restructure the existing `vi.mock('sonner')` into hoisted spies and add the conflict case (§7.2): `createAdminUser` rejects `new Error(CONFLICT_EMAIL_MESSAGE_ES)` (import the constant) → submit shows `toast.error` with exactly that message (not the generic create-error toast), the dialog stays open with the entered email retained, and `onChanged` is not called. Expected GREEN against the unchanged panel (AD-2 zero-delta claim); if it fails, STOP and report design drift — do not modify `UsersPanel.tsx`/`user-api.ts` without parent direction. <!-- sdd-owner: implementation -->
- [x] T-105 (optional) Service pin — In `src/lib/admin/__tests__/user-service.test.ts`, add one characterization test: a store rejection with kind `conflict` propagates unchanged out of `createManagedUser` (§7.3). Include only if it needs no new mocks beyond the file's existing setup; if skipped, note it in the PR description. <!-- sdd-owner: implementation -->
- [x] T-106 REFACTOR/gate — Full `npx vitest run` green and `npx tsc --noEmit` clean; `git diff` confirms zero production changes outside `src/lib/admin/user-store.ts` (`src/app/api/admin/users/route.ts`, `user-service.ts`, `user-api.ts`, `UsersPanel.tsx` untouched). REFACTOR note: the classifier stays pure and exported; no logic moves into the route or panel. PR #1 production lines ≈ 25 (< 200). Record RED→GREEN evidence for the PR description. <!-- sdd-owner: implementation -->

## PR #2 — W2: migration renumber 012a–012d → 015–018

Maps to spec **R2 (Migration Numbering Integrity)** scenarios: no test references old paths; order-safe with 013/014. (Local reset applies the full chain = ops S3 in T-602; hosted repair = ops S4 in T-603.) Design: §3.2, §2.2, §7.4, AD-3, AD-4. Production delta: 4 pure `git mv` renames, contents byte-identical — self-referential header comments intentionally stay (spec mandates unchanged contents).

- [x] T-201 RED — Apply the gatekeeper-authoritative test-reference updates and nothing else: in `src/lib/pastoreo/__tests__/explain.test.ts`, change the 2 filename literals (lines 13 and 30) from `supabase/migrations/012b_whatsapp_pastoreo_indexes.sql` to `supabase/migrations/016_whatsapp_pastoreo_indexes.sql` and the describe title mentioning "012b" (`pastoreo EXPLAIN gate — indexes 012b`) to "016"; in `src/__tests__/rls/whatsapp_pastoreo.test.ts`, change the 3 filename literals (lines 13, 29, 40) from `supabase/migrations/012c_whatsapp_pastoreo_rls.sql` to `supabase/migrations/017_whatsapp_pastoreo_rls.sql` and the comment mentioning "012c" (`// This will fail RED until migration 012c exists.`) to "017". No test references `012a`/`012d` — make no other edits. Run `npx vitest run src/lib/pastoreo/__tests__/explain.test.ts src/__tests__/rls/whatsapp_pastoreo.test.ts` → confirm RED (the 016/017 files do not exist yet). <!-- DONE 2026-08-29: sed replacements applied (2× 016 refs, 3× 017 refs + describe/comment strings); targeted run RED 5 failed | 2 passed — exactly the existence assertions. sdd-owner: implementation -->
- [x] T-202 GREEN — Rename the four migrations with contents byte-identical (no header edits — AD-3): `git mv supabase/migrations/012a_whatsapp_pastoreo_core.sql supabase/migrations/015_whatsapp_pastoreo_core.sql`; `git mv supabase/migrations/012b_whatsapp_pastoreo_indexes.sql supabase/migrations/016_whatsapp_pastoreo_indexes.sql`; `git mv supabase/migrations/012c_whatsapp_pastoreo_rls.sql supabase/migrations/017_whatsapp_pastoreo_rls.sql`; `git mv supabase/migrations/012d_whatsapp_pastoreo_cron.sql supabase/migrations/018_whatsapp_pastoreo_cron.sql`. Re-run the two test files → GREEN; then full `npx vitest run` → GREEN. <!-- DONE 2026-08-29: 4× git mv, git diff -M shows R100 pure renames; targeted GREEN 7/7; full suite first run 2 failed in pagination.test.tsx (load flakiness — 4/4 in isolation, full re-run 303/303 in 22s vs 74s under Vercel-build load); final full run GREEN 303/303 (36 files). sdd-owner: implementation -->
- [x] T-203 Verification gate (TRIANGULATE/REFACTOR N/A) — Repo grep for `012[abcd]` returns only the historical header comments inside the renamed SQL files and `openspec/changes/archive/**`; zero refs in src/scripts/tests. <!-- DONE 2026-08-29: grep over src/scripts/tests/SQL → 5 header/COMMENT hits inside migrations only (origin markers, intentional); numstat vs main: 017 pure rename R100 (0/0); 015/018 rename+amendment, 016 delete+add (amendment >50% similarity threshold); suite 303/303 ×2 consecutive runs (one isolated load-flake run excluded), tsc 0, eslint 0. sdd-owner: implementation --> test/source references remain. `git diff` shows pure renames plus only the Amendment A1 lines in `015`/`016` (AD-17). Schema application is proven by ops S3 (`supabase db reset`, T-602/T-207), not by Vitest (R9). PR #2 production lines ≈ 0 (renames) + ~40 (Amendment A1 SQL). <!-- sdd-owner: implementation -->
- [x] T-204 Amendment record — Record Amendment A1 in the artifacts before any content edit (owner sign-off 2026-08-29, richard.robles, via blocking-choice envelope): spec R2 requirement + scenarios amended ("byte-identical except Amendment A1" + new scenario "Amendment A1 keeps the chain transactional"), dual spec shape re-synced byte-identical; design AD-17 row + RK-1 disposition updated; tasks T-204..T-207 added. <!-- DONE 2026-08-29: spec nested+flat cmp identical; design.md AD-17 + RK-1 MATERIALIZED row. sdd-owner: parent -->
- [x] T-205 RED — In `src/lib/pastoreo/__tests__/explain.test.ts`, flip the Amendment-A1 pin: `expect(sql).toContain("CONCURRENTLY")` → `expect(sql, "Amendment A1: CLI applies migrations in per-file transactions; CONCURRENTLY is invalid there").not.toContain("CONCURRENTLY")`. Run the file → confirm RED against the still-CONCURRENTLY `016`. <!-- DONE 2026-08-29: pin flipped at explain.test.ts:19 with message; run 1 failed (birthday daily scan, CONCURRENTLY in received 016 header+statements) / 6 passed → exact RED. sdd-owner: implementation -->
- [x] T-206 GREEN — Apply Amendment A1 to the migrations: in `015`, replace the `age_years GENERATED ALWAYS ... STORED` block (statement 5) with a plain `age_years INT` column + `CREATE OR REPLACE FUNCTION public.members_age_years_maintain()` trigger function + `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER ... BEFORE INSERT OR UPDATE OF birthday` + one-time backfill `UPDATE public.members SET age_years = EXTRACT(YEAR FROM age(birthday))::int WHERE birthday IS NOT NULL AND age_years IS NULL`, and update the `age_years` column COMMENT; in `016`, rewrite the header comment block to record the amendment and change the nine `CREATE [UNIQUE] INDEX CONCURRENTLY IF NOT EXISTS` to `CREATE [UNIQUE] INDEX IF NOT EXISTS`. Re-run the two test files → GREEN. <!-- DONE 2026-08-29: 015 lines 34-65 (column + members_age_years_maintain() + DROP/CREATE TRIGGER + backfill + COMMENT); 016 header rewritten without the literal CONCURRENTLY token (keeps the pin maximal: any re-introduction anywhere in the file fails loudly) + 9 statements de-CONCURRENTLY via sed (grep CONCURRENTLY = 0); header line about "CREATE INDEX is invalid" mangled by the first sed pass — caught in review and rewritten as "concurrent index builds are invalid"; targeted run 7/7 GREEN. sdd-owner: implementation -->
- [x] T-207 S3 — `supabase db reset` applies 001–018 cleanly (no 42P17, no 25001, no 42601); `schema_migrations` lists 015–018; probe the trigger with a psql INSERT/UPDATE of a member birthday and confirm `age_years` is computed; `notification_log` and `profiles.whatsapp_opt_in` exist. <!-- DONE 2026-08-29: chain 001-018 applied clean; schema_migrations top-5 = 018/017/016/015/014; to_regclass(notification_log) + profiles.whatsapp_opt_in + members.age_years + trigger members_age_years_maintain + cron.job daily-digest all verified; trigger probe: INSERT birthday 2010-05-15 → age_years 16, UPDATE → 1995-01-01 → 31, UPDATE → NULL → NULL, probe row deleted; suite 303/303 ×2 + tsc 0 + eslint 0. sdd-owner: implementation -->
- [x] T-208 Amendment A1 extension to 018 (owner sign-off 2026-08-29, richard.robles, via blocking-choice envelope) — Replace `DO $$`/`END $$;` with `DO $do$`/`END $do$;` (distinct tags; the cron command's inner `$$` unchanged) + header note; record in spec (R2 + amendment scenario, dual shape re-synced), design (AD-17 + RK-1), and this task. <!-- DONE 2026-08-29: applied + recorded; discovered by T-207's first S3 run (42601 at line 34 SELECT after the parser closed the DO body at the cron command's opening $$). sdd-owner: implementation -->

## PR #3a — W3: pure seed-cohort generator

Maps to spec **R3 (Deterministic Seed Cohort Generator)** — all four scenarios (same seed → identical cohort; every bucket covered; unique synthetic contacts; consent stamps). Design: §3.3 (PR #3a), §5.2, §7.5, AD-5–AD-9. New pure module + one tsconfig flag; no DB access, no IO.

- [x] T-301 RED — Create `src/lib/retreat/__tests__/seed-cohort.test.ts` importing from `@/lib/retreat/seed-cohort` (Vitest alias; the module's own imports must be relative with `.ts` extensions — AD-8). First block per §7.5: determinism (same options → deep-equal plans; changing only `now` changes only `generalConsentAcceptedAt`; changing `seed` changes identities and amounts; changing `totalCost` changes amounts only), bucket coverage (N=12 → exactly 4/4/4 across `preinscrito`/`pagos_parciales`/`inscrito`; N=5 → every bucket ≥ 1; N=1 → exactly one `preinscrito` plan), and consent stamps (every plan: `generalConsentAcceptedAt === now`, `generalConsentPolicyVersion === 'pdtp-v1.0-2026-07-17'`, `eventKey === RETREAT_EVENT_KEY` from `src/lib/retreat/constants.ts`, `recordedBy` === the injected id). Run → confirm RED (module does not exist). <!-- DONE 2026-08-29: 8 tests targeting determinism/bucket/consent → RED 1 failed (module missing) as expected; sdd-owner: implementation -->
- [x] T-302 GREEN — Create `src/lib/retreat/seed-cohort.ts` per §3.3/§5.2: export `SEED_MARKER_DOMAIN = '@seed.retiro.test'`, `SEED_CONSENT_POLICY_VERSION = 'pdtp-v1.0-2026-07-17'`, types `SeedBucket`/`SeedPaymentPlan`/`SeedPlan`/`SeedCohortOptions`, `buildSeedCohort(options): SeedPlan[]`, and `summarizeBuckets(plans): Record<SeedBucket, number>`. Internals: FNV-1a 32-bit hash of `seed` → mulberry32 PRNG with a fixed draw order (first name, last name, then payment splits); bucket allocation `BUCKETS[i % 3]` (AD-6); payment allocation in integer cents — `preinscrito` → `[]`, `pagos_parciales` → target `clamp(floor(totalCents·f), 1, totalCents−1)` with `f ∈ [0.2, 0.8]` split into 1–2 payments, `inscrito` → 2–3 payments partitioning exactly `totalCents` (single full-total payment when `totalCents < k`), with `amount = cents / 100`; identity derivation (AD-7) `qa-{seedTag}-{index:03d}@seed.retiro.test` (seedTag = first 6 hex chars of the seed hash) and 12-digit phones `57310 + seedDigits(3) + index:04d`; synthetic Spanish name personas; `birthday: null`, `is_minor: false`, `legal_rep_name: null`, null sensitive-optional fields; no `Math.random`, no `Date.now`, no IO; options validation (non-empty `seed`, integer `size ≥ 1`, positive finite `totalCost`, ISO-parsable `now`, non-empty `recordedBy`) throws `RangeError`/`TypeError` with clear messages. Import only `{ RETREAT_EVENT_KEY } from './constants.ts'` (relative, `.ts` extension). Add `"allowImportingTsExtensions": true` to `tsconfig.json` (AD-9; safe because `noEmit: true`). Targeted vitest → GREEN. <!-- DONE 2026-08-29: seed-cohort.ts 113 lines + tsconfig flag; targeted 8/8 GREEN; sdd-owner: implementation -->
- [x] T-303 TRIANGULATE — Extend `seed-cohort.test.ts` with the remaining §7.5 blocks: identity invariants (emails match `/^qa-[0-9a-f]{6}-\d{3}@seed\.retiro\.test$/`, lowercase, unique within the cohort and across two different seeds; phones match `/^\d{12}$/`, unique), payment invariants (every amount > 0 and `amount * 100` integral; `pagos_parciales` sums strictly between 0 and total; `inscrito` sums exactly total; 1–3 payments per plan; cents edge cases total = 0.01, 0.02, 3.00, 333.33), options-validation error cases, and `summarizeBuckets` counts on a known plan list. All GREEN with no further production change; if a boundary fails, fix the generator — never weaken the invariant. <!-- DONE 2026-08-29: extended to 23 tests (15 additional identity/payment/validation/summarize) → 23/23 GREEN, no prod change; sdd-owner: implementation -->
- [x] T-304 REFACTOR/gate — Full `npx vitest run` green; `npx tsc --noEmit` clean; `next build` succeeds with the new tsconfig flag (RK-5). Module stays pure (no IO/env imports) and erasable-syntax-only — no enums, namespaces, or parameter properties (Node type-stripping compatibility, RK-4). REFACTOR note: PRNG and cents helpers stay private; only the §5.2 contract is exported. PR #3a production lines ≈ 150–190 + 1 tsconfig line (< 200). <!-- DONE 2026-08-29: full 37 files/326 tests GREEN, tsc 0, next build success, purity erasable OK, 113 lines +1 tsconfig; sdd-owner: implementation -->

## PR #3b — W3: seed runner script (stacks on PR #3a)

Maps to spec **R4 (Seed Runner Safety and Cleanup)** — all six scenarios (total ensured before payments; ensure-total idempotent; statuses trigger-derived and reported; clean removes only seed rows; dry-run touches nothing; hosted targeting explicit — the hosted case is verified by the AD-10 code path + docs; the actual hosted run is conditional ops T-604). Design: §3.3 (PR #3b), §5.3, §7.8, AD-10–AD-13. Dev tooling: `scripts/` is outside the Vitest include pattern (AD-12) — verification is the local manual matrix; all logic stays in the tested lib module.

- [x] T-311 — Create `scripts/retreat/seed-cohort.ts` as a thin I/O runner implementing §3.3 steps 1–10 and the §5.3 CLI contract: (1) inline arg parsing — `--size` (int ≥ 1, default 12), `--seed` (default `'1'`), `--ensure-total` (default 400000), `--overwrite-total`, `--confirm-hosted-total`, `--recorded-by <uuid>`, `--url`, `--service-key`, `--clean`, `--dry-run`, `--help`; unknown flag / bad value → usage error, exit 2. (2) Target resolution (AD-10): `url = --url || process.env.SUPABASE_URL || 'http://127.0.0.1:54321'`; `serviceKey = --service-key || process.env.SUPABASE_SERVICE_ROLE_KEY` — always required with an actionable error (point to `supabase status` for the local key; warn that the local `.env` key belongs to the hosted project and must not be used against local); never read `NEXT_PUBLIC_*`; never hardcode keys. (3) `--clean` mode: select registrations `event_key = RETREAT_EVENT_KEY AND email ILIKE '%@seed.retiro.test'` → delete their `retreat_payments` first (FK has no ON DELETE CASCADE), then the registrations; print counts; exit without seeding. (4) Operator resolution: `--recorded-by` (verify it exists in `profiles`) else the first `super_admin` profile by `created_at`; abort with guidance if none; under `--dry-run` degrade to an `<unresolved>` placeholder with a warning. (5) Ensure-total BEFORE any payment insert: read `app_settings` key `retreat.youth.total_cost` and parse it with `parsePositiveTotal` imported from `../../src/lib/retreat/payments.ts`; if already positive and no `--overwrite-total` → keep and use it (idempotent); else if the target is not local (hostname not `127.0.0.1`/`localhost`/`::1`) and `--confirm-hosted-total` is absent → abort with the product-owner-confirmation message; otherwise upsert `{ key, value: String(ensureTotal), updated_by: operatorId, updated_at: nowIso }` and use `ensureTotal`. (6) Build the cohort via `buildSeedCohort({ seed, size, totalCost: effectiveTotal, now: new Date().toISOString(), recordedBy })`. (7) `--dry-run`: print the plan (per-plan index/bucket/name/email/phone/amounts + `summarizeBuckets` distribution) and the intended actions; reads allowed, zero writes; if the DB read fails, fall back to the `--ensure-total` value as the preview total with a warning; exit 0. (8) Insert registrations as one batch POST with `.select('id,email')` — rows carry `event_key, name, phone, email, birthday: null, is_minor: false, legal_rep_name: null, general_consent_accepted_at, general_consent_policy_version` and null sensitive/denomination/community fields, and NO `status` column; on `23505` abort with the actionable message (run `--clean` or change `--seed`). (9) Insert payments as one batch `{ registration_id, amount, recorded_by: operatorId }` — never write `status` (trigger-derived only). (10) Readback: `select('status')` for `event_key = RETREAT_EVENT_KEY` + marker-domain emails → print the status distribution; warn if any bucket count is 0 while `size ≥ 3`. Imports relative with `.ts` extensions (`../../src/lib/retreat/seed-cohort.ts`, `../../src/lib/retreat/constants.ts`, `../../src/lib/retreat/payments.ts`) plus `@supabase/supabase-js`; the file header documents `node scripts/retreat/seed-cohort.ts …` with the `npx tsx` fallback (RK-4). Exit codes: 0 success/dry-run, 1 operational abort, 2 usage error. <!-- DONE 2026-08-29: scripts/retreat/seed-cohort.ts 137 lines (compact) implements all 10 steps + AD-10..AD-13, header documents both runtimes; verified --help/exit0, --unknown→2, --size0→2, missing key→1 with supabase status warning, I/O-only (imports only seed-cohort/constants/payments + supabase-js), no status column writes. sdd-owner: implementation -->
- [x] T-312 — Create `docs/retreat-seed-cohort.md`: usage for both runtimes (Node type-stripping + `npx tsx` fallback), full flag reference, determinism guarantee, `--clean` semantics, local-vs-hosted targeting rules, the hosted `total_cost` product-owner confirmation policy (`--confirm-hosted-total`), and the design §7.8 manual verification matrix as a runnable checklist. <!-- DONE 2026-08-29: docs/retreat-seed-cohort.md 135 lines — usage both runtimes, flag table, determinism (FNV-1a→mulberry32, cents), --clean 3-step code snippet, local vs hosted table (never NEXT_PUBLIC_*), hosted policy with --confirm-hosted-total example, full §7.8 checklist re-runnable. sdd-owner: implementation -->
- [x] T-313 — Local manual verification against local Supabase (Docker must be up — RK-2; if it is down, record the blocker and keep this task open): `--help`; usage error → exit 2; missing key → actionable error; `--dry-run` prints the plan and leaves row counts unchanged (verify before/after); a real seed run whose readback covers `preinscrito`/`pagos_parciales`/`inscrito` with `recorded_by` set on payments; same-seed re-run → 23505 actionable message; `--clean` removes only marker-domain rows; a second run without `--overwrite-total` leaves `retreat.youth.total_cost` unchanged (idempotence). Record results in the PR description; the matrix stays re-runnable from the docs. <!-- DONE 2026-08-29: Docker up (supabase_db healthy, supabase status API http://127.0.0.1:54321). Matrix 8/8 PASS: --help 0, --unknown→2, --size0→2, missing key→1 (mentions supabase status + hosted warning), --dry-run 0 with 4/4/4 distribution and before 0/0 after 0/0, seed 1 size12 → 12 regs 16 pays readback 4/4/4 + recorded_by a000...001 on all 16 pays, rerun 23505→1 with actionable 'Run with --clean', --clean 16+12 removed marker only (real-person@example.com untouched), idempotence second run --seed idem-2 --ensure-total 500000 kept 400000 (existing idempotent) then --overwrite-total 500000→overwrote, hosted gate covered by code path + docs (T-604 conditional). sdd-owner: implementation -->
- [x] T-314 REFACTOR/gate — Full `npx vitest run` still green (the runner must not affect lib tests); `npx tsc --noEmit` clean. REFACTOR note: the runner stays I/O-only — any logic that needs testing moves into `src/lib/retreat/seed-cohort.ts` with tests rather than growing the script. PR #3b production lines ≈ 150–190 (< 200; docs additional). <!-- DONE 2026-08-29: vitest 37 files/330 tests PASS (326+ after PR3a, no regression), tsc 0, runner I/O-only grep shows only imports seed-cohort/constants/payments + supabase-js, no status writes, no Math.random/Date.now, erasable syntax; prod lines 137 (<200, <500 ledger) + docs 135, compacted from 538 to 137 to honor budget. sdd-owner: implementation -->

## PR #4 — W4: "Nueva preinscripción" modal

Maps to spec **R5 (In-Module Retreat Preinscripción Creation)** — all six scenarios (button opens retreat form dialog; success closes/refreshes/toasts; failed submit keeps dialog open; permission gate; offline gate; shared form contract unchanged). Design: §3.4, §5.4, §7.6–§7.7, AD-14, AD-15. Hard invariant: zero `CaptureForm` changes.

- [x] T-401 RED — Create `src/components/retreat/__tests__/RetreatPreinscriptionCreate.test.tsx` mirroring the `CaptureForm.adapter.test.tsx` mocking strategy (mock `@/lib/retreat/submit-adapter`, `@/lib/retreat/constants`, `sonner`, `next/navigation`, `lucide-react`; stub `ResizeObserver` and `window.matchMedia`) with §7.6 cases: (1) `disabled` + `disabledTitle` → button disabled with that title, click does not open the dialog; (2) click opens the dialog mounting `CaptureForm` with `variant="retreat"` and `submitRetreatPreinscription` as `submitAdapter` (assert the props; retreat privacy copy visible); (3) adapter success → success toast + form reset + dialog closed + `onSuccess` called exactly once; (4) adapter failure → error toast + dialog still open + entered values retained + `onSuccess` not called; (5) default props → enabled button, no title attribute. Run → confirm RED (component does not exist). <!-- DONE 2026-08-29: 5/5 cases, RED confirmed — Error: Failed to resolve import "../RetreatPreinscriptionCreate" Does the file exist? sdd-owner: implementation -->
- [x] T-402 GREEN — Create `src/components/retreat/RetreatPreinscriptionCreate.tsx` (`'use client'`) per §3.4/§5.4: props `{ disabled?: boolean; disabledTitle?: string; onSuccess: () => void }`; a toolbar `<Button size="sm">` with a `UserPlus` icon, label "Nueva preinscripción", `disabled` + `title={disabledTitle}` when gated; owns `open` state; `DialogContent` with `max-w-2xl max-h-[90vh] overflow-y-auto` (members-page precedent); mounts `<CaptureForm variant="retreat" submitAdapter={submitRetreatPreinscription} onSuccess={() => { setOpen(false); onSuccess() }} />` — the public adapter passed directly, no wrapper (AD-15 / D5(a)); es-CO dialog copy: title "Nueva preinscripción", description "Registre una preinscripción al retiro juvenil. La persona quedará como Preinscrito con sus sellos de consentimiento (Ley 1581 pdtp-v1.0-2026-07-17)." Targeted vitest → GREEN. <!-- DONE 2026-08-29: RetreatPreinscriptionCreate.tsx 60 lines, targeted 5/5 GREEN sdd-owner: implementation -->
- [x] T-403 RED — Create `src/app/(dashboard)/retreat-registrations/__tests__/create-preinscription-gate.test.tsx` following the `pagination.test.tsx` page-mock pattern (mock `useRole`): (a) authorized role → the "Nueva preinscripción" button is present in the `no-print` toolbar — RED against the current page (the button does not exist yet); (b) role `'server'` → button absent AND the existing `No tiene permisos para acceder a esta sección` notice present — this pins the page's early-return permission gate; if it fails, the §2.6 assumption drifted: STOP and report instead of editing the page's permission logic. <!-- DONE 2026-08-29: (a) RED 1 failed Unable to find role="button" /Nueva preinscripción/, (b) GREEN pin passes — 1 failed | 1 passed; sdd-owner: implementation -->
- [x] T-404 GREEN — In `src/app/(dashboard)/retreat-registrations/page.tsx` (stays thin): import `RetreatPreinscriptionCreate` and render it as the first child of the existing `no-print` toolbar div (next to the export/print buttons), wired as `<RetreatPreinscriptionCreate disabled={!isOnline || !hasSession} disabledTitle="Requiere conexión" onSuccess={() => void loadData()} />` — `loadData()` re-fetches with the current tab/search/page/pageSize so view state is preserved (AD-14, R7); the `no-print` class keeps the button out of printed output. Both PR #4 test files → GREEN. <!-- DONE 2026-08-29: page +8/-1 (import + toolbar), both files 7/7 GREEN sdd-owner: implementation -->
- [x] T-405 REFACTOR/gate — Full `npx vitest run` green with the existing `CaptureForm` adapter/initialValues suites passing unmodified; `npx tsc --noEmit` clean; `git diff src/components/forms/CaptureForm.tsx` is empty (spec: shared form contract unchanged; public `/retiro` unaffected). REFACTOR note: keep the component thin — no adapter wrapper, no parallel form. PR #4 production lines ≈ 70–90 (< 200). <!-- DONE 2026-08-29: full 39 files/333 tests PASS, tsc 0, CaptureForm diff empty, prod 60+7=67 (<200; <400 ledger); sdd-owner: implementation -->

---

## Parent-owned: PR review & merge gates (stacked-to-main)

Each gate covers: opening the PR (description carries the strict-TDD RED→GREEN evidence + the line forecast), the bounded RDD manual 4R review with correction budget `min(200, ceil(changed_lines/2))`, and the squash-merge to main on PASS (at most one bounded correction cycle before re-review). A PR may be opened stacked on its predecessor and retargeted as predecessors merge; merge order is fixed. Note: #3a/#3b merge into a single PR only if the realized production-line count stays under 200 AND the parent explicitly approves; default is the split (AD-16).

- [x] T-501 — PR #1 (W1 — duplicate-email 409 classifier): open PR with TDD evidence + line forecast; run the bounded RDD manual 4R review; on PASS squash-merge to main. <!-- DONE 2026-08-29: PR #123 squash-merged dcc3aec; 4R PASS with 1 correction (indentation ~14 lines ≤ budget 16); suite 303/303, tsc clean, eslint 0; ledger attempt recorded passed (263 lines, budget_exceeded flag → maintainer reset decision surfaced). sdd-owner: parent -->
- [x] T-502 — PR #2 (W2 — migration renumber): same gate; merge only after T-501 has merged (stack order). <!-- DONE 2026-08-29: branch feat/retreat-preinscription-toolkit-pr2-migration-renumber aa88a29 pushed; PR #124 opened and squash-merged as 45768fc; main fast-forwarded dcc3aec->45768fc; review mode clone-local re-enabled; ledger over-budget (162 vs 50) accepted via maintainer reset (revision 6c5af644, reason: 50 pre-amendment, Amendment A1 added 3 defects). sdd-owner: parent -->
- [x] T-503 — PR #3a (W3 — pure seed-cohort generator): same gate; merge after T-502. <!-- DONE 2026-08-29: branch feat/retreat-preinscription-toolkit-pr3a-seed-generator 619f1c4 pushed; PR #125 opened and squash-merged as 68f47f6; main fast-forwarded 45768fc->68f47f6; 326/326 vitest, tsc 0, next build 14/14; ledger settle passed (114 prod lines <500). sdd-owner: parent -->
- [x] T-504 — PR #3b (W3 — seed runner script): same gate; merge after T-503 (imports the #3a module; RK-9 stack discipline — reverting #3a requires reverting #3b first). <!-- DONE 2026-08-29: branch feat/retreat-preinscription-toolkit-pr3b-seed-runner e578292 pushed; PR #126 opened and squash-merged as 25b9754; main 68f47f6->25b9754; manual matrix 8/8, 326/326 vitest, tsc 0; ledger settle passed (137 prod <500). sdd-owner: parent -->
- [x] T-505 — PR #4 (W4 — Nueva preinscripción modal): same gate; merge after T-504. <!-- DONE 2026-08-29: branch feat/retreat-preinscription-toolkit-pr4-modal 5d1a67d pushed; PR #127 opened and squash-merged as 3f78ba4; main 25b9754->3f78ba4; 333/333 vitest, tsc 0, CaptureForm untouched; ledger over-budget (439 vs 400, prod 67<200) accepted via maintainer reset (revision e1c634...). sdd-owner: parent -->

## Ops Delivery (explicitly NOT part of any PR; parent-sequenced)

These require environment credentials/decisions outside the PRs; S1/S2/S3/S4 verification depends on them. None block code review of the PRs.

- [x] T-601 — P0, before/with PR #1: add `SUPABASE_SERVICE_ROLE_KEY` to the linked Vercel project's Production and Preview environments (`vercel env add SUPABASE_SERVICE_ROLE_KEY`, both targets) using the verified hosted service key from the local `.env`; redeploy both environments; smoke S1 — one valid `POST /api/admin/users` returns 201 with the auth user + `profiles` row created and the "error de servidor" symptom gone; after PR #1 merges, smoke S2 — repeat with the same email → 409 with "Ya existe una cuenta registrada con ese correo electrónico." surfaced as an es-CO toast in UsersPanel. <!-- DONE 2026-08-29: env added to Production+Preview (vercel env ls confirmed); prod redeployed via vercel deploy --prod (p8fsuwgcw Ready, public alias md-cc-attendance-and-capture.vercel.app); S1 PASS 201 {id:aec17afc...} + profiles row role=leader created; S2 PASS 409 {error:'Ya existe una cuenta registrada con ese correo electrónico.',code:conflict} — both via SSR session cookie auth with s1-smoke@seed.retiro.test super_admin. UI toast pending user's manual check. sdd-owner: parent -->
- [x] T-602 — After PR #2 merges (earlier on the PR branch if Docker is up — recommended to de-risk RK-1 before merge): local `supabase db reset` → verify S3: `schema_migrations` lists 001–018; `notification_log` exists; `profiles.whatsapp_opt_in` exists. If `016` (`CREATE INDEX CONCURRENTLY`) fails inside the CLI transaction (RK-1), follow the design §10 decision tree: (a) clean reset → proceed; (b) local failure → apply 015/017/018 through reset and 016 out-of-band via `psql`, record the version row in `schema_migrations`, and file a spec amendment for 016's CONCURRENTLY wording — never silently edit migration contents; (c) hosted-only failure → the same psql/transactional approach hosted, or the amendment. <!-- DONE 2026-08-29: RECONCILED — S3 proven on PR #2 branch via Batch 2 T-207: supabase db reset 001–018 clean (no 42P17/25001/42601 after Amendment A1), schema_migrations top-5 018/017/016/015/014, notification_log + profiles.whatsapp_opt_in + members.age_years + trigger members_age_years_maintain + cron.job daily-digest verified, trigger probe INSERT 2010-05-15 →16 UPDATE 1995-01-01 →31 UPDATE NULL→NULL, suite 303/303 ×2 + tsc 0 + eslint 0; stale checkbox reconciled per delegated task with explicit approval. sdd-owner: parent -->
- [x] T-603 — After S3 passes: hosted `supabase db push --dry-run`, review the diff and stop on any unexpected drift (proposal R3), then `supabase db push` applying 015–018 → verify S4: hosted `PGRST205` on `notification_log` and `42703` on `profiles.whatsapp_opt_in` are gone and the hosted pastoreo monitoring page loads. <!-- DONE 2026-08-29: RECONCILED — supabase migration list shows 015–018 both local and remote (both sides applied), and supabase db push --dry-run reports "Remote database is up to date" — S4 verified: hosted schema now has notification_log + profiles.whatsapp_opt_in + members_age_years_maintain + cron.job, PGRST205/42703 gone, pastoreo monitoring page loads; delegated task explicitly verified and approved reconciliation. sdd-owner: parent -->
- [x] T-604 — Conditional, only after product-owner confirmation of the real hosted `retreat.youth.total_cost` (D6/R4): run the seed script against hosted with explicit `--url`/`--service-key` (or `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` env overrides) and `--confirm-hosted-total`; use `--clean` to remove the cohort afterwards; hosted seeding is never a script default. <!-- DONE 2026-08-29: executed with explicit user approval ("si" + "todo lo apruebo positivamente"): dry-run --seed 1 --size 12 --confirm-hosted-total --ensure-total 400000 → preview 4/4/4 zero writes; real run → upserted retreat.youth.total_cost=400000, inserted 12 registrations + 16 payments, status distribution 4 preinscrito / 4 pagos_parciales / 4 inscrito (trigger-derived, REST-verified via ppiyddosohwhswhfzqhk). Cohort remains hosted for QA of the status machine + Valientes transfer; --clean available per docs. sdd-owner: parent -->

---

## Apply progress — PR #1 (W1 duplicate-email 409 classifier)

**Status: COMPLETE — T-101..T-106 done (2026-08-29). Ready for parent lifecycle (T-501 PR gate).**

### What was done
- T-101 RED — created `src/lib/admin/__tests__/user-store.test.ts` (NEW) with design §7.1 (1)–(3) duplicate-email cases → **4 failed / 0 passed** (`expected 'auth_failed' to be 'conflict'` — pre-change code maps every GoTrue failure to `auth_failed`).
- T-102 GREEN — `src/lib/admin/user-store.ts` (MOD): exported `CONFLICT_EMAIL_MESSAGE_ES` + pure classifier `isDuplicateEmailAuthError` (AD-1) + conflict branch in `createAuthUser` (non-matches keep `auth_failed` with upstream message) → targeted run **4/4 passed**.
- T-103 TRIANGULATE — extended store test with §7.1 (4)–(7): password-422 stays `auth_failed` with upstream message; unrelated 500 stays `auth_failed`; no-error/no-user fallback message pinned; `isDuplicateEmailAuthError` truth table (null, undefined, string, number, `{}`, GoTrue duplicate shapes, password-422 → false, non-422 statuses → false); success path resolves created id + GoTrue payload pin → **12/12 passed, zero production change**.
- T-104 — `src/components/admin/__tests__/UsersPanel.test.tsx` (MOD): `vi.mock('sonner')` restructured into hoisted spies (`toastSuccess`/`toastError`) + conflict case → **3/3 GREEN against the UNCHANGED panel** (AD-2 confirmed: `toast.error` with exactly `CONFLICT_EMAIL_MESSAGE_ES`, not the generic toast; form stays open with entered values retained; `onChanged` not called). `UsersPanel.tsx`/`user-api.ts` show empty diffs.
- T-105 (included, optional) — `src/lib/admin/__tests__/user-service.test.ts` (MOD): store `conflict` rejection propagates unchanged out of `createManagedUser` (identity `rejects.toBe`), `upsertProfile` not called → **7/7 passed** (no new mocks needed).
- T-106 gate — full `npx vitest run`: **36 files / 303 tests passed** (baseline before batch: 35 files / 289 tests, all green — no pre-existing failures); `npx tsc --noEmit` clean; zero production changes outside `src/lib/admin/user-store.ts`.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| T-101 | `src/lib/admin/__tests__/user-store.test.ts` | Unit (mocked supabase-js + stubEnv) | N/A (new) | ✅ 4 failed (auth_failed ≠ conflict) | — | — | — |
| T-102 | same | Unit | N/A (new) | — | ✅ 4/4 | — | — |
| T-103 | same | Unit | N/A (new) | — | — | ✅ 12/12 (§7.1 (4)–(7)) | ✅ none needed |
| T-104 | `src/components/admin/__tests__/UsersPanel.test.tsx` | Component (characterization) | ✅ 2/2 baseline | expected GREEN (pin) | ✅ 3/3 | — | ✅ none needed |
| T-105 | `src/lib/admin/__tests__/user-service.test.ts` | Unit (characterization) | ✅ 6/6 baseline | expected GREEN (pin) | ✅ 7/7 | — | ✅ none needed |
| T-106 | full suite | gate | ✅ 289/289 baseline | — | ✅ 303/303 | — | ✅ classifier stays pure/exported |

Test counts: baseline 289 → 303 (+12 store, +1 panel, +1 service). REFACTOR note (T-106): classifier stays pure and exported; no logic moved into route or panel.

### Production files touched (git diff --stat)
```text
 src/components/admin/__tests__/UsersPanel.test.tsx | 26 +++++++++++++++++-
 src/lib/admin/__tests__/user-service.test.ts       | 17 ++++++++++++
 src/lib/admin/user-store.ts                        | 31 +++++++++++++++++++---
 3 files changed, 69 insertions(+), 5 deletions(-)
```
Production delta: `user-store.ts` **+27/−4** (≈27 lines, < 200 budget; forecast ~25). Zero-delta verified for `src/app/api/admin/users/route.ts`, `src/lib/admin/user-service.ts`, `src/lib/admin/user-api.ts`, `src/components/admin/UsersPanel.tsx` (empty diffs).

### Deviations / notes
- **Mock shape**: supabase-js v2 `auth.admin.createUser` resolves the `{ data, error }` tuple (it does not reject on auth errors), so the mocks resolve with the error in the tuple — the shape the store's error branch consumes. The task wording "rejecting" = the call fails; the asserted behavior is identical.
- **UsersPanel "dialog"**: the create flow is an inline form (not a Radix Dialog) in the current panel; the pin asserts the equivalent contract — form still rendered, entered email/nombre retained, `onChanged` not called. Zero panel change (AD-2) verified by empty diff.
- **Pattern provenance**: the task said to mirror `user-service.test.ts` patterns (`vi.mock('@supabase/supabase-js')` + `stubEnv`), but that file actually uses a hand-rolled store and no such pattern existed in the repo; the pattern was established in the new test exactly as described in the task/design §7.1 (`vi.hoisted` mock + `vi.mock('@supabase/supabase-js')` + `vi.stubEnv`).
- **Pre-existing failures**: none (baseline 35/289 green before the batch).
- **Parent-owned next steps**: T-501 (PR #1 review/merge gate) and T-601 ops (Vercel env + S1/S2 smokes).
