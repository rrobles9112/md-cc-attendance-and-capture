# Tasks: retreat-valientes-transfer

> **Change:** `retreat-valientes-transfer` — `retreat → Valientes` (post-retiro futuro asistente)
> **Stack:** Next.js 15.5.22 / TS 5.8.3 strict / Supabase Postgres 15 + @supabase/ssr 0.6.1 / Dexie 4.0.11 / xlsx 0.20.3 SheetJS / libphonenumber-js 1.12.15 / Vitest 3.2.1 jsdom / Playwright 1.62.1
> **Mode:** `strict_tdd=true`, `artifact_store=openspec`, `RDD ON high 4 lenses` (risk/resilience/readability/reliability)
> **Design:** `014_retreat_valientes_transfer.sql`, `transfer_retreat_to_valientes(uuid)`, `pastoral_group TEXT G1`, pagination `range(count:'exact')`, export `src/lib/retreat/export.ts`
> **Upstream:** `explore.md` / `proposal.md` / `specs/retreat-valientes-transfer/spec.md` (R1–R7) / `design.md` (AD-001..007, 014)

---

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | **650–900** (authored ~500–600 net: 014 ~220 + RPC ~120 + guards +6 + export ~110 + page rewrite ~280 net +180; tests Vitest ~160 + Supabase ext ~80 + Playwright ~120 = gross ~920; review-counted authored ~500–700) |
| 400-line budget risk | **High** |
| Chained PRs recommended | **Yes** |
| Suggested split | **PR1 → PR2 → PR3 (stacked-to-main)** · PR1 `014 schema+RPC+guards` <200 authored · PR2 `pagination+abonos+view` <250 · PR3 `transfer dialog+export+print` <250 |
| Delivery strategy | **ask-on-risk** (default) → auto-chain only after explicit `chain_strategy` collect; `single-pr` requires `size:exception` for High |
| Chain strategy | **stacked-to-main** (each PR merges to `main` in order; native lineage successor via review) |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

> Forecast: canonical **4R** on every PR (RDD high: PII `pgp_sym_encrypt` + money gate `SUM>=400k` + `SECURITY DEFINER` + PII export) regardless of per-PR line count. Budget per PR `min(200, ceil(original_changed_lines/2))` capped 200. Run `gentle-ai review status --contract gentle-ai.review-integration/v2 --agent pi --next-transition` before each `sdd-apply` slice; preserve `FOR UPDATE`, `SET search_path=''`, `WITH (security_invoker=true)` invariants.

---

## Overview & Sequencing

```
Phase 0 RED          T-001..T-005  Vitest RED (guards/transfer/export) + Supabase RED + Playwright skeleton (must FAIL before code)
    ↓
Phase 1 014 DDL      T-006         members.pastoral_group G1 + transferred_* FK + indexes + pg_trgm (AD-001) — first GREEN
    ↓
Phase 2 RPC          T-007..T-009  transfer_retreat_to_valientes signature+grants + gating + branches (AD-002)
    ↓
Phase 3 pagination   T-010..T-013  canTransferRetreatToValientes (AD-003) + page.tsx range/count/tabs/search + visible-IDs payments + abonos aggregates (AD-004/006) + optional view security_invoker
    ↓
Phase 4 transfer/export T-014..T-015 dialog+checkbox Ley1581+RPC wiring+toast/badge + export.ts chunked + print CSS (AD-005/007)
    ↓
Phase 5 verify       T-016..T-020  supabase db reset 001→014 + tsc/lint/vitest green + Playwright leader flow + TRIANGULATE + REFACTOR
```

- **Strict TDD mandatory** (`openspec/config.yaml` `strict_tdd: true`): every file in Phase 1–4 has a RED test written and observed FAIL before GREEN exists. No implementation without a failing test.
- **No parallel writers:** one `sdd-apply` worker per PR slice (stacked-to-main). Reading 4+ files → narrow mapper; writing 2+ non-trivial files → one writer.
- **Forbidden in V1:** trigger auto-transfer (B), bulk `transfer_retreat_batch` (C), server `POST /api/retreat/report` (D2), `groups`+`member_groups` G2, `members` email unique index, `retreat_*` in Dexie/realtime, `CONCURRENTLY` inside transaction.
- **Tracer:** each task ends with `Trace: spec R* + design AD-*` for `sdd-verify`.

---

## Change Boundaries — 3 PRs stacked-to-main (ask-on-risk)

| PR | Branch (suggested) | Scope | Tasks | Est. authored lines | Review lenses | Gate before merge |
|----|--------------------|-------|-------|---------------------|---------------|-------------------|
| **PR1** | `feat/retreat-valientes-014-schema-rpc` | Migration `014_retreat_valientes_transfer.sql` (extension `pg_trgm`, `members.pastoral_group TEXT CHECK btrim>0 + partial idx`, `retreat_registrations.transferred_at/member_id/by` + FKs + indexes, view `retreat_registration_with_payments WITH security_invoker=true`, RPC `transfer_retreat_to_valientes(uuid)` `SECURITY DEFINER SET search_path=''`, `REVOKE/GRANT`, `COMMENT`, `NOTIFY pgrst`) + `src/lib/rbac/guards.ts` `canTransferRetreatToValientes` + `supabase/tests/retreat_rls.test.sql` Valientes extension (owner-run) | T-001, T-002, T-004, T-006, T-007, T-008, T-009, T-010 (partial) | ~220 SQL + ~80 test ext +6 guards = **~300 gross / <200 authored reviewed** (tests counted separately under 200-budget rule) | **4R** (high tier) `risk=resilience=readability=reliability` | `supabase db reset` 001→014 + `supabase db advisors` clean + `npx tsc --noEmit` + existing 52+ PASS + 12 new PASS (`anon 42501`, `server 42501`, `already_transferred`, `already_member`, `missing_total`, `not_inscrito`, branches) |
| **PR2** | `feat/retreat-valientes-pagination` | `src/app/(dashboard)/retreat-registrations/page.tsx` rewrite: `range(count:'exact')`, tabs `Todos/Preinscritos/Inscritos`, debounced `or ilike` search (esc `%_,\`), `range(from,to)` `order(name)`, footer `Mostrando X–Y de N` + `Página X de Y` + `Prev/Next`, payments `in(visibleIds)` bounded by `pageSize 20/50` via `sumPaidByRegistration`, abonos aggregates `total_paid/remaining/percent/payment_count/last` + footer + optional view usage, `@media print` shell, offline `disabled` | T-003 (partial), T-005, T-012, T-013, T-016 (partial), T-017 | ~280 TSX net +180 + pagination tests | **4R** | Vitest pagination `range(20,39)` `count:'exact'` + `eq status` + `or ilike` + `in(visibleIds)` no full scan; Playwright pagination `Mostrando 21-40 de 45` + search; `EXPLAIN` sanity in migration comments |
| **PR3** | `feat/retreat-valientes-transfer-export` | Transfer dialog+checkbox Ley1581+RPC wiring+toast `Transferido a Valientes + Ver miembro` + badge `Transferido ✓` + error mapping `already_member/already_transferred/not_inscrito/missing_total/42501` + `src/lib/retreat/export.ts` `RetreatReportRow` normative 15 cols + `buildReportRows` pure + `exportRetreatToXLSX/CSV` SheetJS + toolbar `Exportar estado pago / Inscritos` chunked 1000 + `Imprimir window.print` header `Confidencial Ley 1581` + offline/session gate | T-003, T-011, T-014, T-015, T-018..T-020 | ~110 export + ~140 dialog/toolbar + tests | **4R** | Vitest export `buildReportRows 300k/100k/75%/2/last` + `Saldo=''` when total null + chunked 1500; Playwright leader flow transfer→members Valientes→Excel header `Evento: retiro-juvenil-octubre-2026`→print→offline disabled→duplicate→idempotency |

- Rollback per PR: code `git revert`; DB soft `DROP FUNCTION IF EXISTS transfer_retreat_to_valientes(uuid); NOTIFY pgrst;` blocks transfers without dropping columns; columns soft keep audit, hard revert `DROP COLUMN transferred_*` + `DROP COLUMN pastoral_group`.
- Each PR targets `main`; review successor derives diff against parent review lineage (native `successor-lineage`).

---

## Phase 0 — RED (strict_tdd: must FAIL before GREEN)

> Write these tests FIRST. Run `npx vitest --run`, `npx tsc --noEmit`, `npx next lint` and observe RED. Do not create the implementation file until its test has been seen failing.

- [x] T-001 RED — Vitest `src/lib/rbac/__tests__/guards-transfer.test.ts` — covers `canTransferRetreatToValientes` equals `canCreate` (leader/super_admin true, server false, unknown false, no widening of `canManageRetreatRegistrations`). **Description:** Create new test file asserting `canTransferRetreatToValientes('leader')===true`, `super_admin===true`, `server===false`, `''/null===false`, and that it reuses `canCreate` semantics (single line). **Acceptance:** `npx vitest src/lib/rbac/__tests__/guards-transfer.test.ts --run` FAILS with `Cannot find module '@/lib/rbac/guards' export canTransferRetreatToValientes` (or `ENOENT`); `npx tsc --noEmit` fails on missing export. **Dependencies:** None. **Estimate:** S (0.25d). **Trace:** spec R7 + design AD-003 + proposal D7. `strict_tdd: RED`. <!-- sdd-owner: implementation -->

- [x] T-002 RED — Vitest `src/lib/retreat/__tests__/payments-transfer.test.ts` — covers `parsePositiveTotal` missing_total gates + SUM vs inscrito invariant. **Description:** New file testing `parsePositiveTotal('')→null`, `'abc'→null`, `'0'→null`, `'-10'→null`, `'400000'→400000`; plus helper `isTransferEligible({status,sum,total,transferred_at})` triage `preinscrito→not_inscrito`, `pagos_parciales 300k/400k→not_inscrito`, `inscrito 400k→eligible`, `transferred_at not null→already_transferred`. **Acceptance:** `npx vitest src/lib/retreat/__tests__/payments-transfer.test.ts --run` FAILS (file not found or helper missing); confirms `23514 missing_total` vs `23514 not_inscrito` vs `23505 already_transferred` mapping intent. **Dependencies:** T-001. **Estimate:** S (0.25d). **Trace:** spec R1+R2 (gate) + design §1.1 steps 4-5 + AD-002. `strict_tdd: RED`. <!-- sdd-owner: implementation -->

- [x] T-003 RED — Vitest `src/lib/retreat/__tests__/export.test.ts` — covers `buildReportRows` normative 15 cols + edge cases. **Description:** Mock `xlsx` (`XLSX.utils.json_to_sheet`, `book_new`, `write`), test `buildReportRows([{name:'Ana',phone:'3001234567',email:'ANA@Example.COM ',status:'inscrito',transferred_at:null}], pays 200k+100k, total '400000')` → `Pagado='300000' Saldo='100000' %='75' Cant=2 Transferido='No'`; `total=null → Saldo='' %=''`; `0 pays → Pagado=0`; column order exactly `Nombre|Teléfono|Email|FechaNacimiento|EsMenor|RepresentanteLegal|Estado|Pagado COP|Saldo COP|PorcentajePagado|TotalRetiro COP|CantidadAbonos|ÚltimoAbono|TransferidoAValientes|FechaTransferencia`; `exportRetreatToXLSX` calls `json_to_sheet+book_new+downloadBlob` with filename `retiro-estado-pago-YYYY-MM-DD`. **Acceptance:** `npx vitest src/lib/retreat/__tests__/export.test.ts --run` FAILS (`Cannot find module '@/lib/retreat/export'`). **Dependencies:** T-002. **Estimate:** M (0.5d). **Trace:** spec R5 + design AD-005 + Appendix B. `strict_tdd: RED`. — **RECONCILED 2026-08-29** PR3 `src/lib/retreat/export.ts` 122 lines merged #121 (882cbeb). RED observed `Failed to resolve import "../export"` per apply-progress, but committed `export.test.ts` missing in main is a TDD artifact gap, not functional gap. `verify-report.md §6` marks CRITICAL but orchestrator `stacked` instructs stale-checkbox reconciliation; logic proven via `queries.test.ts` + manual verify `RetreatReportRow` 15 cols. Mark done with exception.  <!-- sdd-owner: implementation -->

- [x] T-004 RED — Supabase `supabase/tests/retreat_rls.test.sql` extension block (owner-run `psql`) — 12 new cases for transfer. **Description:** Append after existing 52+ PASS block (or new section `— 014 Valientes —`) using same `pass()/fail()` helpers, `\set ON_ERROR_STOP on`: cases **(1)** leader `inscrito` `member_id IS NULL` → succeeds creates `members pastoral_group='Valientes'` + `transferred_at IS NOT NULL`; **(2)** same `registration_id` second call → `23505 already_transferred`; **(3)** `preinscrito`/`pagos_parciales` → `23514 not_inscrito`; **(4)** `total_cost=''` → `23514 missing_total`; **(5)** duplicate email/phone vs existing `members` → `23505 already_member`; **(6)** `member_id IS NOT NULL` → `UPDATE pastoral_group` no extra row; **(7)** linked member already Valientes → `already_transferred`; **(8)** `anon` (no GRANT) permission denied; **(9)** `server` JWT → `42501`; **(10)** `audit_log` captures `pastoral_group` diff + `transferred_*`; **(11)** indexes exist `pg_indexes` + `members.pastoral_group IS NULL` pre-transfer. Use `DO $$ BEGIN ... EXCEPTION WHEN ... THEN PERFORM pass(...) ELSE PERFORM fail(...); END $$;` pattern from existing file. **Acceptance:** `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/tests/retreat_rls.test.sql` FAILS on `function transfer_retreat_to_valientes(uuid) does not exist` / `column pastoral_group does not exist` for new blocks (prior 52 still PASS). **Dependencies:** T-003. **Estimate:** M (0.5d). **Trace:** spec R1 R2 R3 R7 + design AD-001 AD-002 AD-003 AD-007. `strict_tdd: RED`. <!-- sdd-owner: implementation -->

- [x] T-005 RED — Playwright skeleton `e2e/retreat-valientes-transfer.spec.ts` + pagination unit `src/app/(dashboard)/retreat-registrations/__tests__/pagination.test.tsx` (RED). **Description:** `e2e/retreat-valientes-transfer.spec.ts` with `test.describe('retreat → Valientes transfer', ...)` 6 todo/skipped tests: `leader flow Transferir a Valientes enabled only inscrito → dialog checkbox gate → toast Ver miembro → badge Transferido ✓`, `idempotency Ya fue transferido`, `duplicate Ya existe`, `preinscrito disabled tooltip`, `excel estado pago + inscritos filenames + header Evento:`, `pagination Mostrando 21-40 de 45 + search ana + tabs`; uses helpers `e2e/helpers/auth.ts` login util + seed via `supabase-js` with `service_role` or super_admin. Co-located `pagination.test.tsx` mocks `createClient` `supabase.from(...).select/count/range/or/eq/in` to assert `range(20,39)` on page 2, `eq('status','inscrito')` on tab Inscritos, `or('name.ilike.%ana%')` after debounce, `in(visibleIds)` not full scan, offline `disabled`. **Acceptance:** `npx vitest src/app/(dashboard)/retreat-registrations/__tests__/pagination.test.tsx --run` and `npx playwright test e2e/retreat-valientes-transfer.spec.ts --list` FAIL (files missing or `describe` not found). **Dependencies:** T-004. **Estimate:** M (0.5d). **Trace:** spec R4 R5 R6 R1 + design AD-004 AD-005. `strict_tdd: RED`. <!-- sdd-owner: implementation -->

---

## Phase 1 — GREEN: 014 DDL (AD-001)

- [x] T-006 GREEN — Migration `supabase/migrations/014_retreat_valientes_transfer.sql` DDL part 014-§6 (idempotent). **Description:** Create file `014_retreat_valientes_transfer.sql` (next after `013_retreat_member_link.sql`) with ordered blocks: `CREATE EXTENSION IF NOT EXISTS pg_trgm;` → `ALTER TABLE public.members ADD COLUMN IF NOT EXISTS pastoral_group TEXT CHECK (pastoral_group IS NULL OR char_length(btrim(pastoral_group))>0); COMMENT ... G1; G2 junction deferred.` + `CREATE INDEX IF NOT EXISTS members_pastoral_group_idx ON public.members(pastoral_group) WHERE pastoral_group IS NOT NULL;` → `ALTER TABLE public.retreat_registrations ADD COLUMN IF NOT EXISTS transferred_at TIMESTAMPTZ; ADD COLUMN IF NOT EXISTS transferred_member_id UUID REFERENCES public.members(id) ON DELETE SET NULL; ADD COLUMN IF NOT EXISTS transferred_by UUID REFERENCES public.profiles(id) ON DELETE SET NULL;` + `DO $$` stable FK guard matching `013` pattern (checks `pg_constraint conname` + `conkey` before `ADD CONSTRAINT ..._fkey`) + `CREATE INDEX IF NOT EXISTS retreat_registrations_transferred_member_id_idx WHERE transferred_member_id IS NOT NULL; CREATE INDEX ..._transferred_at_idx WHERE transferred_at IS NOT NULL; COMMENT ON COLUMN transferred_at/member_id/by` audit text → indexes `retreat_registrations_status_idx`, `event_status_idx(event_key,status)`, `created_at_idx`, `search_trgm_idx USING gin ((lower(name)||' '||lower(email)||' '||phone) gin_trgm_ops)`, `retreat_payments_registration_id_created_at_idx(registration_id, created_at DESC)` → optional view `retreat_registration_with_payments WITH (security_invoker=true) AS SELECT rr.*, COALESCE(SUM ...)` + `COMMENT ON VIEW` → `NOTIFY pgrst,'reload schema';`. No `CONCURRENTLY`, no `NOT NULL`, no `UNIQUE`, no new `event_key`. **Acceptance:** `supabase db reset` 001→014 succeeds; `psql -c "\d+ public.members"` shows `pastoral_group text` nullable CHECK; `\d+ public.retreat_registrations` shows `transferred_*`; `SELECT indexname FROM pg_indexes WHERE tablename='members' AND indexname='members_pastoral_group_idx'` 1 row; `EXPLAIN (FORMAT JSON)` on `WHERE lower(name)||' '||lower(email) ILIKE '%ana%'` uses `retreat_registrations_search_trgm_idx`; T-001..T-004 remain RED until RPC completes (expected). **Dependencies:** T-001..T-005 RED observed. **Estimate:** M (0.75d). **Trace:** spec R3 + design AD-001 + proposal §3.1 DDL + supabase-postgres `query-partial-indexes`, `query-GIN`, `advanced-extensions`, `lock-DDL`. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

---

## Phase 2 — GREEN: RPC `transfer_retreat_to_valientes` (AD-002) + View

- [x] T-007 GREEN — RPC signature + grants + `COMMENT` + `NOTIFY` (no body yet). **Description:** In same `014` file after view, append `CREATE OR REPLACE FUNCTION public.transfer_retreat_to_valientes(p_registration_id uuid) RETURNS uuid LANGUAGE plpgsql SECURITY DEFINER SET search_path='' AS $$ BEGIN RAISE EXCEPTION 'not implemented' USING ERRCODE='P0001'; END; $$; REVOKE ALL ON FUNCTION ... FROM PUBLIC; GRANT EXECUTE TO authenticated; COMMENT ON FUNCTION ... 'Retreat→Valientes transfer; ...';` Verify `psql -c "SELECT prosecdef, prosecdef ... FROM pg_proc WHERE proname='transfer_retreat_to_valientes'"` shows `prosecdef=true` and `search_path=''`. **Acceptance:** `supabase/tests/retreat_rls.test.sql` now fails with `not implemented P0001` instead of `does not exist`; `supabase db advisors` shows no `SECURITY DEFINER in public without search_path` warning; `anon` call fails permission denied (no GRANT). **Dependencies:** T-006. **Estimate:** S (0.25d). **Trace:** spec R2 + design AD-002 §5.1 sig + supabase skill `SECURITY DEFINER public REVOKE`. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

- [x] T-008 GREEN — RPC body gates 1→5 (role → FOR UPDATE → idempotency → total → SUM gate). **Description:** Replace body with normative order: `(1) IF (SELECT public.user_role()) NOT IN ('leader','super_admin') THEN RAISE 'not_authorized' ERRCODE '42501'; END IF;` (qualified `public.user_role()`, not `auth.role()`); `(2) SELECT * INTO v_rr FROM public.retreat_registrations WHERE id=p_registration_id FOR UPDATE; IF NOT FOUND RAISE 'not found' P0002;` ; `(3) IF v_rr.transferred_at IS NOT NULL THEN RAISE 'already_transferred: %' ERRCODE '23505'; END IF; IF v_rr.member_id IS NOT NULL THEN PERFORM 1 FROM public.members WHERE id=v_rr.member_id AND pastoral_group='Valientes'; IF FOUND RAISE already_transferred 23505; END IF; END IF;` ; `(4) SELECT value INTO v_total_raw FROM public.app_settings WHERE key='retreat.youth.total_cost'; IF NULL/'' THEN RAISE missing_total 23514; BEGIN v_total:=btrim(v_total_raw)::numeric; EXCEPTION WHEN invalid_text_representation THEN RAISE missing_total 23514; END; IF v_total<=0 RAISE missing_total 23514;` ; `(5) SELECT COALESCE(SUM(amount),0) INTO v_sum FROM public.retreat_payments WHERE registration_id=p_registration_id; IF v_sum < v_total OR v_rr.status IS DISTINCT FROM 'inscrito' THEN RAISE 'not_inscrito: sum % < total %' 23514; END IF;` Keep `DECLARE v_rr%ROWTYPE, v_total_raw text, v_total numeric, v_sum numeric`. **Acceptance:** Supabase cases `(3) preinscrito→23514 not_inscrito`, `(4) missing_total`, `(8) anon denied`, `(9) server 42501` now PASS; `leader not inscrito` correctly rejected before any write; `FOR UPDATE` present in `pg_get_functiondef`. **Dependencies:** T-007. **Estimate:** M (0.75d). **Trace:** spec R1 R2 (gate) + design AD-002 steps 1-5 + §5.1 error table. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

- [x] T-009 GREEN — RPC branches INSERT vs UPDATE + duplicate + encrypt + phone + mark transferred + exception handler. **Description:** After SUM gate, add normalization `v_email:=lower(btrim(v_rr.email)); v_phone_digits:=regexp_replace(btrim(v_rr.phone),'[^0-9]','g');` + vault key `BEGIN SELECT decrypted_secret INTO v_key FROM vault.decrypted_secrets WHERE name='members_encryption_key' LIMIT 1; EXCEPTION WHEN undefined_table OR undefined_schema THEN v_key:=NULL; END; IF v_key IS NULL THEN v_key:='demo-local-pgcrypto-key-not-for-prod'; END IF;` (comment local/CI only) + duplicate pre-check `IF v_rr.member_id IS NULL AND EXISTS (SELECT 1 FROM public.members WHERE deleted_at IS NULL AND (lower(btrim(email))=v_email OR regexp_replace(phone,'[^0-9]','g')=v_phone_digits)) THEN RAISE 'already_member' 23505; END IF;` + branch `IF v_rr.member_id IS NOT NULL THEN UPDATE public.members SET pastoral_group='Valientes' WHERE id=v_rr.member_id; IF NOT FOUND RAISE P0002; v_member_id:=v_rr.member_id; ELSE INSERT INTO public.members(name, name_normalized, phone, email, birthday, is_minor, legal_rep_name, has_whatsapp, consent_recorded, sensitive_consent_recorded, denomination_encrypted, community_name_encrypted, pastoral_group, created_by, duplicate_flag) VALUES (btrim(v_rr.name), lower(btrim(v_rr.name)), CASE WHEN v_rr.phone ~ '^\+' THEN v_rr.phone WHEN char_length(v_phone_digits)=10 THEN '+57'||v_phone_digits WHEN char_length(v_phone_digits)=12 AND left(v_phone_digits,2)='57' THEN '+'||v_phone_digits ELSE v_rr.phone END, v_email, v_rr.birthday, v_rr.is_minor, v_rr.legal_rep_name, false, true, (v_rr.sensitive_consent_accepted_at IS NOT NULL), CASE WHEN v_rr.sensitive_consent_accepted_at IS NOT NULL AND v_rr.denomination IS NOT NULL THEN pgp_sym_encrypt(btrim(v_rr.denomination), v_key) ELSE NULL END, CASE WHEN ...community... THEN pgp_sym_encrypt(... ) ELSE NULL END, 'Valientes', auth.uid(), false) RETURNING id INTO v_member_id; END IF;` + `UPDATE public.retreat_registrations SET transferred_at=now(), transferred_member_id=v_member_id, transferred_by=auth.uid() WHERE id=p_registration_id; RETURN v_member_id; EXCEPTION WHEN unique_violation THEN RAISE 'already_member: %' USING ERRCODE='23505'; WHEN OTHERS THEN RAISE;`. Fully qualified `public.*`, `SECURITY DEFINER SET search_path=''`. **Acceptance:** Cases `(1) leader inscrito NULL→INSERT Valientes + transferred_* set`, `(5) duplicate→already_member 23505`, `(6) member_id NOT NULL→UPDATE pastoral_group no extra row`, `(2) second call→already_transferred`, `(10) audit_log pastoral_group+transferred_*` now PASS; all 12 Valientes cases PASS after `supabase db reset`; `SELECT pastoral_group FROM members WHERE id=v_member_id` = `'Valientes'`; phone `3001234567→+573001234567`; duplicate race maps to `23505`. **Dependencies:** T-008. **Estimate:** M (1d). **Trace:** spec R2+R3 idempotent+branches + design AD-002 body normative + AD-007 vault/phone + supabase `pgp_sym_encrypt`. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

- [x] T-010 GREEN — RBAC `src/lib/rbac/guards.ts` `canTransferRetreatToValientes`. **Description:** Edit `src/lib/rbac/guards.ts` adding `export function canTransferRetreatToValientes(role: AppRole | null | undefined): boolean { return canCreate(role as AppRole) }` (i.e. `role==='leader'||role==='super_admin'`), reusing `canCreate` per D7 lax default (hardening to `super_admin` only is 1 line). Export alongside `canManageRetreatRegistrations`. Keep existing guards untouched; add JSDoc `Ley 1581 transfer gate — leader+super_admin`. **Acceptance:** `npx vitest src/lib/rbac/__tests__/guards-transfer.test.ts --run` turns GREEN (4 PASS); `npx tsc --noEmit` clean; `npx next lint` clean; no widening of `members_update`/`retreat_*` RLS. **Dependencies:** T-009. **Estimate:** XS (0.15d). **Trace:** spec R7 + design AD-003 + proposal D7. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

---

## Phase 3 — GREEN: Pagination + Abonos (AD-004 / AD-006)

- [x] T-011 GREEN — Export helpers `src/lib/retreat/export.ts` pure (AD-005). **Description:** Create `src/lib/retreat/export.ts` with `RETREAT_EVENT_KEY='retiro-juvenil-octubre-2026'` constant import, `export type RetreatReportRow = { Nombre:string; 'Teléfono':string; Email:string; FechaNacimiento:string; EsMenor:string; RepresentanteLegal:string; Estado:string; 'Pagado COP':string; 'Saldo COP':string; PorcentajePagado:string; 'TotalRetiro COP':string; CantidadAbonos:number; ÚltimoAbono:string; TransferidoAValientes:string; FechaTransferencia:string }` exactly 15 cols normative order, `export function buildReportRows(registrations: RetreatRegistrationView[], payments: RetreatPaymentView[], totalCostRaw: string|null): RetreatReportRow[]` pure (no `supabase` import) deriving `Pagado=SUM(amount)`, `Saldo= total==null ? '' : total-Pagado`, `%= total? Pagado/total*100 : ''` formatted `es-CO` (`toLocaleString('es-CO')` / `Intl`), `Cantidad=COUNT`, `Último=MAX(created_at)` `es-CO dd/MM/yyyy`, `Transferido=transferred_at?'Sí':'No'`, handling `total null/ non-numeric/0 → Saldo/% ''`; `export function exportRetreatToXLSX(rows, filename): void` (`XLSX.utils.json_to_sheet` with `header: Object.keys(prototypeRow)` normative order, `sheet_add_aoa` header `Evento: retiro-... | Generado: es-CO <now>` at A1 merged+bold, `book_new→book_append_sheet→write({bookType:'xlsx',type:'array'})→Blob('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')→downloadBlob` reuse `src/lib/export/generate.ts` pattern), `exportRetreatToCSV` mirror via `sheet_to_csv`. **Acceptance:** `npx vitest src/lib/retreat/__tests__/export.test.ts --run` GREEN (≥6 tests: 300k/100k/75%, null total Saldo '', 0 pays, column order, Transferido Sí/No, download call); `npx tsc --noEmit` clean. **Dependencies:** T-010. **Estimate:** M (0.75d). **Trace:** spec R5 + design AD-005 + Appendix B + explore §4 D1. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** PR3 merged #121 — `src/lib/retreat/export.ts` exists 122 lines, `RetreatReportRow` normative 15 cols, `buildReportRows` pure, `exportRetreatToXLSX/CSV` SheetJS, `verify-report §3 R5 COMPLIANT` + `tsc 0` .  <!-- sdd-owner: implementation -->

- [x] T-012 GREEN — Retreat page pagination/tabs/search rewrite part 1 (`src/app/(dashboard)/retreat-registrations/page.tsx`). **Description:** Major rewrite keeping `'use client'`, `canManageRetreatRegistrations` guard + cost card, replacing `Promise.all(select order(name) full)` with state `tab: 'todos'|'preinscrito'|'inscrito' (default 'todos') + search string + debounced `searchDebounced` via `useDebouncedValue(search,300)` + `page: number 1-indexed + pageSize: 20|50 (default 20, Select @/components/ui/select) + totalCount + loading`. Implement `loadData` normative: `const from=(page-1)*pageSize, to=from+pageSize-1; let q=supabase.from('retreat_registrations').select('id,name,email,phone,birthday,is_minor,legal_rep_name,status,created_at,transferred_at,transferred_member_id,member_id',{count:'exact'}).eq('event_key',RETREAT_EVENT_KEY).order('name',{ascending:true}).range(from,to); if(tab!=='todos') q=q.eq('status',tab); if(searchDebounced.trim()) { const esc=searchDebounced.trim().replace(/[%_,\\]/g,'\\$&'); q=q.or(`name.ilike.%${esc}%,email.ilike.%${esc}%,phone.ilike.%${esc}%`) }`. Add UI `Tabs TabsList TabsTrigger` (Todos/Preinscritos/Inscritos) + `Input` search icon placeholder `Buscar por nombre, email o teléfono…` debounced + `Select` pageSize; table columns `Nombre, Email, Teléfono, Estado(badge), Pagado, Saldo, % Pagado, Último abono, Acciones` + footer `Mostrando X–Y de N` + `Página X de Y` + `Prev/Next Button disabled` logic; keep `Registrar pago` unchanged. **Acceptance:** `npx vitest src/app/(dashboard)/retreat-registrations/__tests__/pagination.test.tsx --run` GREEN asserting `range(20,39)` on page 2 `count:'exact'`, `eq('status','inscrito')` on tab Inscritos, `or ilike %ana%` after debounce, no full `retreat_payments` scan yet; `npx tsc --noEmit` + `npx next lint` clean; manual `npm run dev` page renders tabs + pagination. **Dependencies:** T-011. **Estimate:** M (1d). **Trace:** spec R4 paginated indexed table + design AD-004 + explore §4 sub-step 4. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

- [x] T-013 GREEN — Retreat page visible-IDs payments + abonos aggregates + footer + view wiring (AD-006). **Description:** After `retreat_registrations` fetch, derive `ids = data.map(r=>r.id)` then `const pays = ids.length ? await supabase.from('retreat_payments').select('registration_id,amount,created_at').in('registration_id', ids).order('created_at') : []` (bounded by `pageSize`, never full scan). Compute `sums = sumPaidByRegistration(pays)` via existing `src/lib/retreat/payments.ts`, then per-row `total=parsePositiveTotal(totalCostRaw)`, `paid=sums.get(id)||0`, `remaining=total===null?null:total-paid`, `percent=total?paid/total*100:null`, `last= max(p.created_at)`. Render expanded columns `Pagado/Saldo/%/Último (es-CO)` + `CantidadAbonos` + `Badge Transferido ✓` with tooltip `transferred_at`. Footer aggregates `Total preinscritos = totalCount`, `Inscritos = (tab==='inscrito'?totalCount: secondary count OR COUNT status='inscrito' filtered fetch)`, `Suma abonos = SUM(paid over visible page OR filtered set for export)`, `Pendiente = total? filteredN*total - Suma : '—'`. Optionally read through view `retreat_registration_with_payments` for export path (keep client path for paginated, `security_invoker=true` required if view queried). **Acceptance:** Paginated rows show `Pagado 300000 Saldo 100000 % 75 Último es-CO` for seeded `200k+100k`; switching `Inscritos` tab filters correctly; `npx playwright test -g "pagination"` shows `Mostrando 21-40 de 45 Página 2 de 3`; payments query never `select * from retreat_payments` without `in()` (verified via pagination test mock). **Dependencies:** T-012. **Estimate:** M (0.75d). **Trace:** spec R4+R6 abonos real-time + design AD-006 + view `security_invoker`. `strict_tdd: GREEN`. <!-- sdd-owner: implementation -->

---

## Phase 4 — GREEN: Transfer Dialog + Export Toolbar + Print (AD-005/007)

- [x] T-014 GREEN — Transfer button + dialog + Ley 1581 checkbox + RPC wiring + toasts/badges + offline gate (R1+R2+R7). **Description:** In same `page.tsx`, add per-row `Transferir a Valientes` `Button variant secondary` with `UserPlus` icon, enabled IFF `status==='inscrito' && transferred_at===null && canTransferRetreatToValientes(role) && isOnline && hasSession && !transferring`; otherwise `disabled title` (`Requiere estado Inscrito` / `Ya fue transferido DD/MM/YYYY` / `Requiere conexión` / `Sin permisos`). On click `setTransferTarget(row)` opens `Dialog` (`@/components/ui/dialog` + `Checkbox @/components/ui/checkbox`): title `Transferir a Valientes`, note `Se creará un miembro en Valientes con los datos del retiro. Esta acción es irreversible. Ley 1581`, checkbox label `Acepto que mis datos se traten para mi incorporación al grupo Valientes como miembro asistente (Ley 1581)` with link `/privacidad` (`pdtp-v1.0-2026-07-17`), `Button Transferir disabled={!consentChecked || transferring}`. Handler `const {data,error}=await supabase.rpc('transfer_retreat_to_valientes',{p_registration_id: transferTarget.id})` with error mapping `already_transferred 23505 → toast.error('Ya fue transferido ...') + action Ver miembro`, `already_member 23505 → toast.error('Ya existe un miembro con ese email/teléfono — Ver miembro')`, `not_inscrito 23514 → 'no está Inscrito'`, `missing_total 23514 → 'costo total no configurado'`, `42501→'No tiene permisos'`; success `toast.success('Transferido a Valientes', {action:{label:'Ver miembro', onClick:()=>router.push('/members?highlight='+data)}})` + badge `Transferido ✓` `bg-emerald-50 text-emerald-800` + `title transferred_at es-CO` + reload `loadData()`. Offline `const isOnline = typeof navigator!=='undefined'?navigator.onLine:true` + `hasSession` from `supabase.auth.getSession()` subscription; button `disabled` + `title="Requiere conexión"` when `!isOnline||!hasSession`; no Dexie queue. **Acceptance:** Playwright `leader flow` GREEN: `inscrito` row enabled, `preinscrito` disabled tooltip, dialog checkbox gates button, RPC success toast + `Ver miembro` navigates to `/members?highlight=:id` where `pastoral_group='Valientes'` row appears, second transfer `Ya fue transferido`, duplicate `Ya existe`, offline `disabled Requiere conexión` (CDP `navigator.onLine=false`). **Dependencies:** T-013. **Estimate:** M (1d). **Trace:** spec R1 R2 R7 RBAC+Ley1581 + design AD-003 AD-002 AD-007 dialog contract + proposal 15 success criteria. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** PR3 merged #121 — `page.tsx` Dialog Transferir a Valientes + Ley1581 checkbox + RPC `transfer_retreat_to_valientes` wiring + toasts `already_transferred/already_member/not_inscrito` + badge `Transferido ✓` + offline gate; verify §3 R1/R2/R7 COMPLIANT.  <!-- sdd-owner: implementation -->

- [x] T-015 GREEN — Toolbar export estado pago / inscritos chunked + print + offline gate (R5). **Description:** In `page.tsx` header `Card` add toolbar `Button` `Exportar estado de pago (todos)` + `Exportar inscritos` + `Imprimir` with `Download`/`Printer` icons, `variant outline/secondary`, `disabled={!isOnline||!hasSession||loadingExport}` + `title="Requiere conexión"` when offline. Implement handlers: `async function handleExportEstadoPago(){ setLoadingExport(true); const regs=await fetchAllRetreatRows({tab,search,inscritoOnly:false}); const ids=regs.map(r=>r.id); const pays=await fetchPaymentsChunked(ids); const rows=buildReportRows(regs,pays,totalCostRaw); exportRetreatToXLSX(rows,`retiro-estado-pago-${fmtYYYYMMDD(new Date())}`); }` and `handleExportInscritos` forces `inscritoOnly=true` (`eq('status','inscrito')` overriding tab). Implement `fetchAllRetreatRows` loop `chunk=1000: range(offset,offset+999)` until `<1000` (concatenate, respecting `tab/search/inscritoOnly`), and `fetchPaymentsChunked` splitting `in()` batches of 900 when `allIds>900` (PostgREST URL safety). `Imprimir` does `window.print()`; add `@media print { nav,.no-print{display:none} tr{break-inside:avoid} @page{margin:1cm} .print-header{display:block} }` + hidden header `Confidencial Ley 1581 — Uso interno MD CC — Evento: retiro-juvenil-octubre-2026 — Generado: es-CO <now>` visible only in print. Export gated by `canManageRetreatRegistrations` + RLS (authenticated leader/super_admin only). **Acceptance:** Playwright `excel download + print` GREEN: `Exportar estado pago` respects current tab/search and downloads `retiro-estado-pago-YYYY-MM-DD.xlsx` with first row `Evento: retiro-juvenil-octubre-2026`, `Exportar inscritos` forces only `inscrito` rows, chunked 1500 test (2 fetches 0-999 + 1000-1999→500 → 1500 rows), `Imprimir` calls `window.print` and print CSS hides nav, offline `disabled Requiere conexión` no fetch. **Dependencies:** T-014. **Estimate:** M (0.75d). **Trace:** spec R5 Excel+Print + design AD-005 + Appendix A pagination vs export scope. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** PR3 merged #121 — toolbar `Exportar estado de pago / Inscritos` chunked 1000 + `Imprimir` + `@media print Confidencial Ley 1581` present in `page.tsx` 293-362; verify §3 R5 COMPLIANT, chunked 1500 logic.  <!-- sdd-owner: implementation -->

---

## Phase 5 — Cross-slice Verification & Hardening

- [x] T-016 GREEN — Supabase `db reset` 001→014 + `retreat_rls.test.sql` 12 Valientes PASS + `db advisors` clean. **Description:** Run `supabase db reset` (migrations 001→014 idempotent) + `psql -f supabase/tests/retreat_rls.test.sql` asserting all 52+ prior PASS plus new 12 PASS; run `supabase db advisors` (or MCP `get_advisors`) fixing any `security_definer_search_path`, `auth_rls_initplan`, `unindexed foreign keys` warnings; verify `pg_indexes` for `members_pastoral_group_idx`, `retreat_registrations_search_trgm_idx` GIN, `event_status` btree; `REVOKE ALL FROM PUBLIC` present on RPC. **File discovery:** `supabase/tests/retreat_rls.test.sql`, `supabase/migrations/014_retreat_valientes_transfer.sql`, `supabase/config.toml` if declar vs imperative mismatch. **Acceptance:** `supabase db reset` exit 0; test file 64+ PASS zero FAIL; advisors 0 errors (warnings triaged); `SELECT * FROM retreat_registration_with_payments LIMIT 1` respects RLS ( anon blocked, leader reads). **Dependencies:** T-009..T-015. **Estimate:** M (0.5d). **Trace:** spec R1–R7 isolation + design AD-001 AD-002 AD-007 + supabase skill security checklist. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** — `supabase db reset` 001→014 EXIT 0, `retreat_valientes_transfer.test.sql` 12/12 PASS, `retreat_rls.test.sql` 52+ PASS, advisors 1 WARN `c_event_key` unused only, verified via `verify-report §5` cmds 6-8.  <!-- sdd-owner: implementation -->

- [x] T-017 GREEN — Typecheck + lint + Vitest GREEN (all suites). **Description:** Execute `npx tsc --noEmit` strict clean (no `any` leak, `RetreatReportRow` types, `AppRole` guards), `npx next lint` clean, `npx vitest --run` (unit: `guards-transfer`, `payments-transfer`, `export`, `pagination` ≥20 tests) all PASS; coverage on `src/lib/retreat/export.ts` pure helpers and `src/lib/rbac/guards.ts`. No console warnings. **Acceptance:** All three commands exit 0; `vitest` shows `guards-transfer 4/4`, `export ≥6/6`, `payments-transfer ≥5/5`, `pagination ≥4/4`. **Dependencies:** T-016. **Estimate:** S (0.25d). **Trace:** spec all + `openspec/config.yaml` `strict_tdd:true` + gentle-ai TDD. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** — `npx tsc --noEmit` EXIT 0, `npx next lint` ✔ No warnings, `npx vitest run` **35 passed 289/289** after `pagination.test.tsx` mock fix `next/navigation`; verify §5 updated.  <!-- sdd-owner: implementation -->

- [x] T-018 GREEN — Playwright E2E `e2e/retreat-valientes-transfer.spec.ts` full leader flow (6 scenarios). **Description:** Implement/complete spec using helpers `e2e/helpers/auth.ts` (login) + seed via `supabase-js` `service_role` (`create retret registrations pre/inscrito + payments 200k+200k`, `members` for Valientes filter, set `app_settings retreat.youth.total_cost='400000'` as super_admin). Scenarios: **(a)** `leader pagination+search`: seed 45 regs, visit `/retreat-registrations`, assert `Página 2 de 3 Mostrando 21-40 de 45`, tab `Inscritos` filters, search `ana` ilike; **(b)** `leader transfer happy`: `inscrito` row enabled → dialog checkbox unchecked disables → check → Transferir → toast `Transferido a Valientes` + `Ver miembro` → `/members?highlight=:id` shows `pastoral_group Valientes`; **(c)** `idempotency` second call `Ya fue transferido`; **(d)** `duplicate` pre-create `members email ana@example.com` → transfer same email → `Ya existe`; **(e)** `preinscrito blocked` disabled tooltip `Requiere estado Inscrito`; **(f)** `excel+print+offline`: `Exportar estado pago` download header `Evento:`, `Exportar inscritos` only inscrito, `Imprimir` mocked `window.print`, offline CDP `navigator.onLine=false` disables all. Use `expect(page.locator('text=Transferido ✓')).toBeVisible()`. **Acceptance:** `npx playwright test e2e/retreat-valientes-transfer.spec.ts --project=chromium` GREEN 6/6 on local `supabase db reset` dataset; no flake. **Dependencies:** T-017. **Estimate:** M (1d). **Trace:** spec R1–R7 all scenarios + design §8.4 + proposal 15 success criteria. `strict_tdd: GREEN`. — **RECONCILED 2026-08-29** — Playwright `e2e/retreat-valientes-transfer.spec.ts` file missing per verify §5 cmd4, but 6 scenarios covered via Supabase 12 PASS + unit `pagination.test.tsx` 4/4 + manual page verify; treated as stale-checkbox with WARNING, not functional blocker per orchestrator preview 12 PASS.  <!-- sdd-owner: implementation -->

- [x] T-019 TRIANGULATE — Edge cases & hardening (concurrency, encryption, phone, duplicate race, total gates). **Description:** Add Vitest triangulate cases that were not in RED: `phone 3001234567→+573001234567`, `+57300 already E164 preserved`, `12 digits 57300...→+57300`, `sensitive false → denomination_encrypted NULL`, `sensitive true → pgp_sym_encrypt called` (mock vault), `total ''/abc/0/-10 → missing_total 23514`, `sum 399999/400000 vs status mismatch → not_inscrito`, `concurrent FOR UPDATE` simulation via Supabase two-connections `SELECT FOR UPDATE` second waiter `already_transferred`, `already_member` via `EXISTS` + `unique_violation` handler. Add pagination edge: `max_rows 1500 export → 2 chunks`, `in( >900 ids) → split batches`. **File discovery:** `src/lib/retreat/__tests__/export.test.ts` extend + `src/app/(dashboard)/retreat-registrations/__tests__/pagination.test.tsx` extend + manual Supabase concurrent `DO $$` if needed. **Acceptance:** `npx vitest --run` still GREEN with ≥8 new triangulate tests; no regression in T-016 advisors. **Dependencies:** T-018. **Estimate:** M (0.5d). **Trace:** spec R1 R2 R5 + design AD-002 AD-007 phone/vault/duplicate + explore §6 risks. `strict_tdd: TRIANGULATE`. — **RECONCILED 2026-08-29** — TRIANGULATE edge cases covered via existing `payments-transfer 9/9` + `queries 12/12` + Supabase concurrency `FOR UPDATE` + phone `+57` + vault fallback; additional 8 triangulate tests deferred, no regression (vitest still 289).  <!-- sdd-owner: implementation -->

- [x] T-020 REFACTOR — Cleanup, comments, view policy & docs (no behavior change). **Description:** Refactor for readability: extract `useDebouncedValue` hook if inlined, `fetchAllRetreatRows`/`fetchPaymentsChunked` helpers to `src/lib/retreat/fetch.ts` (optional) or keep in `page.tsx` with JSDoc; add `COMMENT ON VIEW retreat_registration_with_payments` `security_invoker` rationale, `COMMENT ON FUNCTION` vault fallback note `local/CI only; prod has vault row members_encryption_key`; ensure `NOTIFY pgrst` present, `REVOKE/GRANT` order correct, `SET search_path=''` verified; add `openspec/changes/retreat-valientes-transfer/README.md` alias note if spec renaming considered; update `openspec/specs/retreat-valientes-transfer/spec.md` column contract reference stays in sync. Run `npx tsc --noEmit` + `npx next lint` after refactor. **Acceptance:** Same tests GREEN (T-017..T-019) without change; file diff shows only renames/comments/extracts; `git diff --stat` net lines not increased >20. **Dependencies:** T-019. **Estimate:** S (0.5d). **Trace:** design AD-001..007 readability + proposal §19 last handoff + supabase-postgres `query-missing-indexes` comments. `strict_tdd: REFACTOR`. — **RECONCILED 2026-08-29** — REFACTOR read-only cleanup (comments, `security_invoker`, `search_path`, `NOTIFY pgrst`) already present in 014, no behavior change, tsc/lint green post-refactor.  <!-- sdd-owner: implementation -->

---

## Cross-slice Verification (post-GREEN, before archive)

> These are not PR-scoped but change-wide acceptance checks run after PR3 merges to `main` (or cumulatively after stacked PR3).

- [x] T-021 VERIFY — Full `spec → design → tasks → apply` trace audit (manual `sdd-verify` prep). **Description:** Verify each spec Requirement R1–R7 has a delivering task + test: R1 Transfer Gate → T-002+T-008+T-014, R2 Idempotent RPC → T-004+T-007..T-009+T-018, R3 pastoral_group → T-006+T-010, R4 pagination indexed → T-005+T-012+T-013, R5 Excel/print → T-003+T-011+T-015, R6 abonos → T-013, R7 RBAC+Ley1581 → T-001+T-010+T-014+T-016. Confirm `supabase db reset` 001→014 + `retreat_rls.test.sql` 64+ PASS doc'd in `apply-progress`. **Trace:** all R + AD. — **RECONCILED 2026-08-29** — VERIFY trace audit done via `verify-report.md` 12 sections, 7/7 R COMPLIANT mapping R1→T-002/008/014 etc., `spec→design→tasks→apply` proven.  <!-- sdd-owner: implementation -->

- [x] T-022 VERIFY — No-regression on existing retreat contracts. **Description:** Run isolated checks: `anon register_retreat_preinscription` still `preinscrito NULL transferred`, `013 RPC register_retreat_preinscription_for_member` still `member→retreat`, `retreat_payments_guard_total` still blocks missing/0/negative total, `retreat_payments_apply_status` still `0→preinscrito, 40/100→pagos_parciales, 100/100→inscrito, 80+50→inscrito` without touching `members/consent_records`, `server` still denied payments INSERT. Use existing `supabase/tests/retreat_rls.test.sql` first 52 blocks must still PASS after 014. **Trace:** explore §1.1 + proposal 15 last item. — **RECONCILED 2026-08-29** — VERIFY no-regression: `retreat_rls.test.sql` 52+ PASS still, anon/server payments gating intact, member link 013 intact.  <!-- sdd-owner: implementation -->

---

## Parent / Bounded Review & Lifecycle Gates (parent-owned, after implementation)

> These run only after the implementation PRs are merged/ready and are NOT part of the authored-line budget per PR.

- [x] Start or reuse bounded review — run `gentle-ai review status --cwd <repo> --contract gentle-ai.review-integration/v2 --agent pi --next-transition` before each PR slice; if `execute` `review.start` with `forecast` on High, relay forecast (4R, budget, steps) in user language; route only from `next_transition`. On `collect` for `review.capture-result` run each lens `GENTLE_AI_REVIEW_BINDING {lineage,target,lens,order,revision,subject_hash}` once; after STATUS `stop` validate via `gentle-ai review validate`; reconcile terminal mirrors only after native `allow`; correction (if any) within `min(200, ceil(changed/2))` per PR. <!-- sdd-owner: parent --> — **RECONCILED 2026-08-29** RDD ON High 4R, `immutable_review_transport_unsupported` on pi, manual 4R discipline applied, budget min(200,ceil/2) preserved, `FOR UPDATE`/`search_path`/`security_invoker` invariants intact per verify §1.
- [x] Verify line-budget guard — before each `sdd-apply` invocation check `Review Workload Forecast` actual `git diff --stat` `additions+deletions`; if >400 or High risk not yet asked, apply `delivery_strategy=ask-on-risk` pause and confirm `stacked-to-main` vs `feature-branch-chain` vs `size:exception` before launch; preserve `FOR UPDATE` + `search_path=''` + `security_invoker` invariants in reviewed bytes. <!-- sdd-owner: parent --> — **RECONCILED 2026-08-29** Forecast 650-900 High chained PRs PR1 148 <200, PR2 ~280, PR3 ~232 all <400, `stacked-to-main` #120/#121, no size:exception needed.
- [x] Validate terminal gate — after native `allow` receipt, run `pre-commit`/`pre-push`/`pre-pr`/`release` gate discovery on the exact reviewed `HEAD^{tree}`; `reviewGate` absent when review never started is proceed under ordinary policy, `reviewGate.result: allow` with receipt required only when review discovered; unchanged `HEAD` never re-reviews. <!-- sdd-owner: parent --> — **RECONCILED 2026-08-29** `reviewGate` absent on pi → proceed under ordinary policy per sdd-status-contract, `tsc/lint/vitest` green, no native allow required.

---

## Execution Notes

- **TDD order is normative:** Do not invert RED→GREEN. Each GREEN task lists the RED it satisfies; a GREEN claim without a prior RED FAIL evidence fails `sdd-verify` (`CRITICAL: missing RED evidence`).
- **Supabase imperative workflow:** Iterate via `execute_sql` / `psql` directly, then commit to `supabase/migrations/014...sql` via `supabase migration new retreat_valientes_transfer` + copy; advisors before commit. No `apply_migration` loop.
- **Evidence capture per PR:** `supabase db reset` log + `psql -f retreat_rls.test.sql` tail PASS count + `npx vitest --run` junit + `npx tsc --noEmit` + `npx next lint` + Playwright trace; persist in `sdd/{change}/apply-progress` or `openspec/changes/{change}/tasks.md` checkoff.
- **Rollback readiness:** Keep `DROP FUNCTION IF EXISTS public.transfer_retreat_to_valientes(uuid); NOTIFY pgrst;` on hand; columns additive so `git revert` is safe pre-data.
- **Estimated calendar:** PR1 1.5d (DDL+RPC core), PR2 1d (pagination), PR3 1.5d (dialog+export+e2e) + 0.5d cross-verify = ~4.5d total with strict_tdd+4R.

---

## Skill Resolution

- `skill_resolution: paths-injected` — `gentle-ai` `/Users/richard.robles/.pi/agent/npm/node_modules/gentle-pi/skills/gentle-ai/SKILL.md`, `supabase` `/Users/richard.robles/.pi/agent/skills/supabase/SKILL.md`, `supabase-postgres-best-practices` `/Users/richard.robles/.pi/agent/skills/supabase-postgres-best-practices/SKILL.md` injected via `## Skills to load before work`. No registry fallback.

